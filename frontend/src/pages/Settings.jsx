import React,{useState} from 'react';
export default function Settings({user}){const [notifications,setNotifications]=useState(true);const [saved,setSaved]=useState(false);return <div className="page-content"><div className="page-intro"><div><h2>Settings</h2><p>Personalize your analytics workspace.</p></div></div><section className="settings-grid"><div className="card settings-card"><h3>Profile</h3><label>Name<input defaultValue={user?.name||''}/></label><label>Email<input defaultValue={user?.email||''} disabled/></label><button className="primary-button" onClick={()=>setSaved(true)}>Save changes</button>{saved&&<small className="saved">Changes saved locally for this session.</small>}</div><div className="card settings-card"><h3>Dashboard Preferences</h3><label>Default date range<select defaultValue="All time"><option>All time</option><option>Last 30 days</option><option>Last 90 days</option></select></label><label>Default region<select defaultValue="All"><option>All</option><option>West</option><option>East</option><option>Central</option><option>South</option></select></label><div className="toggle-row"><div><b>Email notifications</b><span>Receive important report reminders.</span></div><button className={`toggle ${notifications?'on':''}`} onClick={()=>setNotifications(!notifications)} aria-label="Toggle notifications"><i/></button></div></div></section></div>}
