import React,{useEffect,useState} from 'react';
import { Download, Printer } from 'lucide-react';
import { api } from '../services/api';
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(Number(n)||0);
function downloadCSV(rows){const header=Object.keys(rows[0]||{});const csv=[header.join(','),...rows.map(r=>header.map(k=>`"${String(r[k]??'').replaceAll('"','""')}"`).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='sales-report.csv';a.click();URL.revokeObjectURL(a.href)}
export default function Reports(){const [summary,setSummary]=useState({}),[rows,setRows]=useState([]);useEffect(()=>{Promise.all([api.summary({}),api.transactions({})]).then(([s,t])=>{setSummary(s);setRows(t)})},[]);return <div className="page-content"><div className="page-intro"><div><h2>Reports</h2><p>Generate a simple report from the live MySQL-backed analytics API.</p></div><div className="button-row"><button className="small-button" onClick={()=>downloadCSV(rows)}><Download size={15}/> Export CSV</button><button className="small-button" onClick={()=>window.print()}><Printer size={15}/> Print / PDF</button></div></div><section className="report-preview card"><div className="report-header"><div><span className="eyebrow">Sales Analytics</span><h2>Performance Report</h2><p>Generated from the current database snapshot.</p></div><strong>{new Date().toLocaleDateString()}</strong></div><div className="report-metrics"><div><span>Revenue</span><b>{money(summary.totalRevenue)}</b></div><div><span>Orders</span><b>{summary.totalOrders||0}</b></div><div><span>Units</span><b>{summary.unitsSold||0}</b></div><div><span>AOV</span><b>{money(summary.averageOrderValue)}</b></div></div><div className="report-note">This report uses the public transaction dataset loaded into MySQL. It is not live company data.</div></section></div>}
