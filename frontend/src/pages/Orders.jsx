import React,{useEffect,useState} from 'react';
import { Search } from 'lucide-react';
import { api } from '../services/api';
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(Number(n)||0);
export default function Orders(){const [rows,setRows]=useState([]),[q,setQ]=useState(''),[region,setRegion]=useState('All'),[regions,setRegions]=useState([]); useEffect(()=>{api.options().then(o=>setRegions(o.regions||[]))},[]); useEffect(()=>{const id=setTimeout(()=>api.orders({search:q,region}).then(setRows).catch(()=>setRows([])),250);return()=>clearTimeout(id)},[q,region]);return <div className="page-content"><div className="page-intro"><div><h2>Orders</h2><p>Order-level aggregation from the transaction table.</p></div></div><div className="toolbar"><div className="inline-search"><Search size={17}/><input placeholder="Search order, customer or product" value={q} onChange={e=>setQ(e.target.value)}/></div><select value={region} onChange={e=>setRegion(e.target.value)}><option>All</option>{regions.map(r=><option key={r}>{r}</option>)}</select></div><section className="card table-card"><div className="table-scroll"><table><thead><tr><th>Order ID</th><th>Customer</th><th>Product</th><th>Quantity</th><th>Amount</th><th>Region</th><th>Ship Mode</th><th>Date</th></tr></thead><tbody>{rows.map(r=><tr key={r.orderId}><td className="mono">{r.orderId}</td><td>{r.customer}</td><td className="truncate">{r.product}</td><td>{r.quantity}</td><td><b>{money(r.amount)}</b></td><td>{r.region}</td><td><span className="status neutral">{r.shipMode}</span></td><td>{r.date}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">No orders found.</div>}</div></section></div>}
